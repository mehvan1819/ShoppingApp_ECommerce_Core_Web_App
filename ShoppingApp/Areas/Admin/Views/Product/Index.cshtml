@model ShoppingApp_Models.ViewModel.ProductVM;


@{
    ViewData["Title"] = "Index";
}

<div class="container p-4">
    <div class="row p-3">
        <div class="col-6 text-primary">
            <a class="btn btn-primary" asp-action="CreateUpdate" asp-controller="Product"><i class="bi bi-patch-plus"></i>&nbsp; Create New Product</a>
        </div>       
    </div>
</div>

<table id="tbldata">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Description</th>
            <th>Price</th>
            <th>Category</th>
            <th>Action</th>
        </tr>
    </thead>
    @*<tbody>
        @foreach (var item in Model.ProductList)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category.CategoryName)
                </td>
                <td>
                    <a class="btn btn-primary" asp-action="CreateUpdate" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>&nbsp; Edit</a> |
                    <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-card-list"></i>&nbsp; Details</a> |
                    <a class="btn btn-primary" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>&nbsp; Delete</a>
                </td>
            </tr>
        }
    </tbody>*@
</table>

@section scripts{

    <script src="~/js/product.js"></script>
}


